# FutBotMX UAQ Team — SAM 3 stack
# Python 3.11. Torch instalado por separado (CPU o CUDA segun pod).
# CPU pod: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# GPU pod (RTX 5090 Blackwell): pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128

# Core scientific
numpy>=1.26,<2.3
scipy>=1.11
pandas>=2.1
matplotlib>=3.8
pillow>=10.0

# Vision + video
opencv-python-headless>=4.9
decord>=0.6.0
av>=12.0
moviepy>=1.0.3
imageio>=2.34
imageio-ffmpeg>=0.4.9
scikit-image>=0.22

# ML + deep learning ecosystem
transformers>=4.45
accelerate>=0.34
peft>=0.13
huggingface_hub>=0.25
safetensors>=0.4.5
timm>=1.0
einops>=0.8

# Tracking / detection utilities
supervision>=0.22

# Config + utilities
omegaconf>=2.3
hydra-core>=1.3
pyyaml>=6.0
tqdm>=4.66
rich>=13.7

# Jupyter ecosystem
jupyterlab>=4.2
notebook>=7.2
ipywidgets>=8.1
ipykernel>=6.29

# Dev tools
black>=24.4
ruff>=0.6
pytest>=8.2

# SAM 3 - instalar manual (no en PyPI todavia):
# pip install git+https://github.com/facebookresearch/sam3.git
# O usar version Hugging Face cuando este disponible
