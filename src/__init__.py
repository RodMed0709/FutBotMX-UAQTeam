"""Paquete raiz del proyecto FutBotMX (UAQ Team, Copa FutBotMX).

Agrupa los submodulos del proyecto (p. ej. ``src.utils``, ``src.core``). Se
instala en modo editable (``pip install -e .``) para poder importarse desde
cualquier parte del proyecto, incluidos los notebooks, sin manipular ``sys.path``.
"""
